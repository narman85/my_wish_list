import 'package:flutter/material.dart';

class AddWishScreen extends StatelessWidget {
  const AddWishScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}